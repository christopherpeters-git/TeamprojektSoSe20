(WINDOWS)
1. Set online mode in Communication.js to true
2. Execute docker_containers_build.bat
3. Execute docker_containers_run.bat

The docker containers are now build and running

to stop the containers:
docker_containers_stop.bat